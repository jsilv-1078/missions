import type {Metadata} from 'next'
export const metadata:Metadata={title:'Collection',description:'Track the value and performance of your Card Madness collection.'}
export default function Layout({children}:{children:React.ReactNode}){return children}
