import type {Metadata} from 'next'
export const metadata:Metadata={title:'Gauge',description:'Research one card with value, range, liquidity, trend and grading context.'}
export default function Layout({children}:{children:React.ReactNode}){return children}
