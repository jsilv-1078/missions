'use client'

import {useEffect,useMemo,useState} from 'react'
import AppNav from '../components/AppNav'
import './gauge.css'

type Payload={card?:{cardId:string;title:string;subtitle?:string;imageUrl?:string;grade:string;currentFmv?:number};comps?:Array<{id:string;price:number;soldAt?:string;source?:string;url?:string}>;trendSummary?:{percentChange?:number;direction?:string;salesUsed?:number};liquidity?:{score:number;label:string;sales30:number;sales90:number;lastSaleDays:number};range30?:{low:number;high:number};gradingOpportunity?:{score?:number;recommendation:string;raw?:number;psa9?:number;psa10?:number;gradingCost:number;psa9Net?:number;psa10Net?:number};error?:string}
const grades=['Raw','PSA 10','PSA 9','SGC 10']
const money=(n?:number)=>n==null?'—':`$${n.toLocaleString('en-US',{maximumFractionDigits:0})}`

export default function GaugePage(){
 const [mode,setMode]=useState<'quick'|'scan'|'cert'>('quick'),[q,setQ]=useState(''),[cert,setCert]=useState(''),[grade,setGrade]=useState('Raw'),[loading,setLoading]=useState(false),[data,setData]=useState<Payload|null>(null),[scanPreview,setScanPreview]=useState('')
 useEffect(()=>{const v=new URLSearchParams(window.location.search).get('q');if(v)setQ(v)},[])
 const trend=useMemo(()=>data?.trendSummary?.percentChange,[data])
 async function run(){if(mode==='scan'){setData({error:'Scan Card is in beta. Your photo is selected, but automatic image identification is not connected yet. Use Search Card or PSA Cert for a live result.'});return}setLoading(true);setData(null);try{const sp=new URLSearchParams();if(mode==='cert')sp.set('cert',cert);else sp.set('q',q);sp.set('grade',grade);const r=await fetch(`/api/gauge?${sp.toString()}`,{cache:'no-store'});setData(await r.json())}catch{setData({error:'Gauge is temporarily unavailable.'})}finally{setLoading(false)}}
 function chooseScan(file?:File){if(!file)return;setScanPreview(URL.createObjectURL(file));setData(null)}
 const summary=data?.card?`${money(data.card.currentFmv)} estimated value · ${data.liquidity?.label??'Liquidity unavailable'} · ${trend==null?'Trend unavailable':`${trend>=0?'Rising':'Falling'} ${Math.abs(trend).toFixed(1)}%`}`:''
 return <main className="gauge-shell"><header className="gauge-header"><div><p>GAUGE</p><h1>Know before you buy.</h1></div></header><section className="gauge-search"><div className="mode-row"><button className={mode==='quick'?'on':''} onClick={()=>setMode('quick')}>Search Card</button><button className={mode==='scan'?'on':''} onClick={()=>setMode('scan')}>Scan Card · Beta</button><button className={mode==='cert'?'on':''} onClick={()=>setMode('cert')}>PSA Cert</button></div>{mode==='quick'?<textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Paste or type a card listing"/>:mode==='cert'?<input value={cert} onChange={e=>setCert(e.target.value)} placeholder="PSA certificate number"/>:<div className="scan-panel"><label className="scan-upload"><img src={scanPreview||'/IMG_4838.jpeg'} alt="Scan a card"/><span>{scanPreview?'Card photo selected':'Take or choose a card photo'}</span><input type="file" accept="image/*" capture="environment" onChange={e=>chooseScan(e.target.files?.[0])}/></label><p>Photo recognition is being tested. Search Card and PSA Cert return live results now.</p></div>}<label>GRADE</label><div className="grade-row">{grades.map(g=><button key={g} className={grade===g?'on':''} onClick={()=>setGrade(g)}>{g}</button>)}</div><button className="find" onClick={run} disabled={loading}>{loading?'Checking market…':mode==='scan'?'Continue':'Find card'}</button></section>{!data&&!loading?<section className="gauge-intro"><h2>One card. Four answers.</h2><div className="intro-grid"><span><b>Value</b>Current market value</span><span><b>Range</b>Recent sold range</span><span><b>Liquidity</b>How easily it sells</span><span><b>Grade Check</b>Potential grading upside</span></div></section>:null}{data?.error?<div className="error">{data.error}</div>:null}{data?.card?<><section className="metric-card gauge-summary"><label>GAUGE SUMMARY</label><h2>{summary}</h2></section><section className="result-card">{data.card.imageUrl?<img src={data.card.imageUrl} alt=""/>:<div className="no-image">CARD</div>}<div className="result-info"><div className="grade-display"><span>GRADE</span><strong>{data.card.grade}</strong></div><h2>{data.card.title}</h2><p>{data.card.subtitle}</p></div></section><section className="metric-card hero"><label>MARKET VALUE</label><h2>{money(data.card.currentFmv)}</h2><p>Current estimated value for {data.card.grade}</p><div className="range"><span>Recent low<b>{money(data.range30?.low)}</b></span><span>Recent high<b>{money(data.range30?.high)}</b></span></div></section><section className="metric-card"><div className="metric-head"><div><label>LIQUIDITY</label><h2>{data.liquidity?.score?.toFixed(1)??'—'}<small>/10</small></h2></div><strong>{data.liquidity?.label??'—'}</strong></div><p>{data.liquidity?`${data.liquidity.sales90} sales in 90 days · latest sale ${data.liquidity.lastSaleDays===0?'today':`${data.liquidity.lastSaleDays}d ago`}`:'Not enough sales history'}</p></section><section className="metric-card"><label>90-DAY TREND</label><h2 className={(trend??0)>=0?'up':'down'}>{trend==null?'—':`${trend>=0?'+':'−'}${Math.abs(trend).toFixed(1)}%`}</h2><p>Based on the earliest vs. most recent median sales windows.</p></section>{data.gradingOpportunity?<section className="metric-card grade-check"><div className="metric-head"><div><label>GRADE CHECK</label><h2>{data.gradingOpportunity.score?.toFixed(1)??'—'}<small>/10</small></h2></div><strong className={data.gradingOpportunity.recommendation==='GRADE'?'positive':'negative'}>{data.gradingOpportunity.recommendation}</strong></div><div className="grade-values"><span>Raw<b>{money(data.gradingOpportunity.raw)}</b></span><span>PSA 9<b>{money(data.gradingOpportunity.psa9)}</b></span><span>PSA 10<b>{money(data.gradingOpportunity.psa10)}</b></span></div><p>Uses current market values and an estimated $80 grading cost. Card condition is not assessed.</p></section>:null}<section className="sales"><div className="sales-head"><label>RECENT SALES</label><span>{data.comps?.length??0} shown</span></div>{(data.comps??[]).slice(0,5).map((s,i)=><div className="sale-row" key={`${s.id}-${i}`}><b>{money(s.price)}</b><span>{s.soldAt?new Date(s.soldAt).toLocaleDateString():'Recent'}</span><span>{s.source??'Sold'}</span></div>)}</section></>:null}<div className="nav-space"/><AppNav active="gauge"/></main>
}
