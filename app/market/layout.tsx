import type {Metadata} from 'next'
export const metadata:Metadata={title:'Market',description:'Track Card Madness trading-card market indexes by sport.'}
export default function Layout({children}:{children:React.ReactNode}){return children}
